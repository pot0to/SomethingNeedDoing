fix some exceptions when adding/removing files
ensure config importing works
get rid of chatmanager
fix up help menu
	- lua should be more like an actual docs
	- functions/clicks etc could fill editor if it has focus
ensure us in ar post process button works (also make a migration?)
see if the waiters can be removed
check on the crafting side of snd (actions list etc hasn't been updated for DT (is it even needed?))
excel
	- language selection
	- in-sheet search
	- resolve cell data from other sheets
	- ensure packedbools are checked correctly
other language support for macros (c#, python?)